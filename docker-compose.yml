version: "3.9"

services:
  discord-bot:
    build: .
    container_name: discord-bot
    env_file:
      - .env              # โหลดค่าตัวแปรจากไฟล์ .env
    restart: unless-stopped
    volumes:
      - .:/app            # ให้ container เห็น source code ปัจจุบัน (สะดวกตอน dev)
    tty: true
